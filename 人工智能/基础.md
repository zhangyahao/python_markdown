1. 按模型功能与输出类型分类：
   1. 回归：预测连续的数值。
   2. 生成：学习训练数据的分布，并生成新的、符合该分布的数据样本。
   3. 分类：将数据划分到预定义的类别中。
   4. 聚类：将物理或抽象对象的集合分组为由类似的对象组成的多个类。
   5. 降维：在保留数据核心特征的前提下，减少数据的特征数量（维度），用于数据压缩或可视化。
2. 激活函数：
   1.  Sigmoid 函数
       *  
